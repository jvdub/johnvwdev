version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 24.16.0
        - nvm use 24.16.0
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
